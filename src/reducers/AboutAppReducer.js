const INITIAL_STATE = { aboutApp : '', policy: '', loader : false };export default (state = INITIAL_STATE, action) => {	switch (action.type) {		case 'getAboutApp':			return {				aboutApp: action.payload.data,				loader: action.payload.success			};		case 'getPolicy':			return {				policy: action.payload.data,				loader: action.payload.success			};		default:			return state;	}};